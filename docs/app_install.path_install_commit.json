c({"name": "app_install.path_install_commit", "type": "attribute", "parents": ["app_install"], "comment": "<h3>Path: <code>/install/commit</code></h3>\n\n<p>This accepts data submitted from the installation wizard and does the actual\nsetup of the system.</p>\n\n<p>Once it has successfully completed, this will terminate the installer, \nallowing the parent <code>install.sh</code> script to launch the main appDev server.</p>", "src": "appDev/install/app_install.js", "line": 542, "children": []})